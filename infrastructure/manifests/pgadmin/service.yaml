apiVersion: v1
kind: Service
metadata:
  name: pgadmin
  namespace: dl-infra
  labels:
    app.kubernetes.io/name: pgadmin
    app.kubernetes.io/part-of: digitallead-platform
spec:
  type: ClusterIP

  selector:
    app.kubernetes.io/name: pgadmin

  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
